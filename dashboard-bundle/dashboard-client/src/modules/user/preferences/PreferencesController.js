/**
 * Controller for the all customers screen
 * @param $scope
 * @param $location
 * @constructor
 */
var PreferencesController = function($scope, $location) {


    /**
     * Invoked on startup, like a constructor.
     */
    var init = function(){

    };
    init();
};

PreferencesController.$inject = ['$scope',  '$location'];
module.exports = PreferencesController;